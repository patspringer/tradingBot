{
  "name": "tdAmeritradeBot",
  "version": "0.1.0",
  "private": true,
  "homepage": "",
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.17.2",
    "react": "^16.4.1",
    "request": "^2.88.2"
  },
  "scripts": {
    "start": "node App.js",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
